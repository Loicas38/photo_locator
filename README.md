# Photo locator

## From_trace

This script aims at locating pictures using the track(s) which can be contained into a gpx file. It takes the point whch is the closest from the time at which the picture was taken to decide where it was taken. Multiple variables allow to set some parameters.